# Ozon-1688 选品系统 — 数据缺口矩阵与修复方案

> 生成: 2026-04-28 | 基准: 232张真实Ozon类目卡片 | 来源: Claude + Codex 联合分析

## 一、数据缺口矩阵(按依赖链排序)

### Ozon 可爬取字段(俄文页面)

| # | 字段 | 当前填充 | 数据源 | 俄文关键词/选择器 | 修复策略 |
|---|------|---------|--------|-----------------|---------|
| 1 | SkuId | 93.5% | 列表URL | `-/\\d{5,}/` | 已有, 容错增强 |
| 2 | 标题 | 90.9% | 列表 span | `tsBody400Small`, 最长候选 | 已有, 已修复嵌套span |
| 3 | 绿标价 | 90.9% | 列表 `₽` | `(\d[\d\s\u2009]*)\\s*₽` | 已有 |
| 4 | 黑标价 | 93.5%* | 列表 `<s>`/`<del>` | `скидк`, `line-through` | 已修复: 划线检测, 无则null |
| 5 | 评级 | 46.1% | 列表SVG | `отзыв`(50次), `рейтинг` | 已修复: 宽松匹配+容差0.05 |
| 6 | 评论数 | **提取但丢弃** | 列表 `отзыв` | `(\d+)\\s*(?:отзыв\\|оцен\\|review)` | **P0**: normalize增加字段 |
| 7 | 一级类目 | 0% | JSON-LD BreadcrumbList | `Электроника`, `Телефоны и смарт-часы` | **P0**: JSON-LD解析 |
| 8 | 三级类目 | 0% | JSON-LD BreadcrumbList[-1] | `BreadcrumbList[].name` | **P0**: 同上 |
| 9 | 品牌 | 0% | 详情规格/列表 `Оригинал` | `бренд`(4次), `Бренд` | **P1**: 详情页提取 |
| 10 | 店铺名称 | 0% | 详情页 `продавец` | `продавец`, `Перейти в магазин` | **P1**: 详情页(列表页已无data-seller) |
| 11 | 跟卖数量 | 0% | 详情offer列表 | `Есть быстрее`, `Другие продавцы`, `предложени` | **P0**: 详情页提取, 评分关键 |
| 12 | 最低跟卖价 | 0% | 详情offer | `от ... ₽` | **P1** |
| 13 | 配送方式 | 0% | 详情badge | `доставк`(9次), `Доставка` | **P1** |
| 14 | 重量(克) | 0% | 详情规格表 | `Вес товара`, `Вес с упаковкой` | **P1**: 物流费关键 |
| 15 | 尺寸(mm) | 0% | 详情规格表 | `Длина/Ширина/Высота` | **P1**: 物流费关键 |

### 1688 可爬取字段(中文页面)

| # | 字段 | 当前填充 | 数据源 | 中文关键词 | 修复策略 |
|---|------|---------|--------|-----------|---------|
| 16 | 采购价 | 0% (仅7 SKU CSV) | 1688详情页 | `¥`/`￥`, `起批` | **P0**: Playwright爬取 |
| 17 | 运费 | 0% (仅默认值) | 1688运费模板 | `运费`, `快递`, `物流` | **P0**: 同上 |
| 18 | 供应商名 | 0% | 1688店铺信息 | `供应商`, `公司`, `厂家` | **P2** |
| 19 | 成交记录 | 0% | 1688详情 | `成交`, `已售`, `笔` | **P2** |
| 20 | 起批量 | 0% | 1688详情 | `起批量`, `起订量`, `≥` | **P2** |

### 数学建模预估字段

| # | 字段 | 当前填充 | 预估公式 | 依赖 | 优先级 |
|---|------|---------|---------|------|--------|
| 21 | 月销量_est | 0% | `0.6×Δ评论30天/评论比 + 0.3×排名模型 + 0.1×库存下降` | 评论数, 采集时间戳 | P0 |
| 22 | 转化率_est | 0% (已有_est但输入不完整) | 见 §二 | 评分, 评论数, 价格, 竞争 | P0 |
| 23 | Ozon物流费 | 0% | `tariff[配送方式,计费重,尺寸档]` | 重量, 尺寸, 配送方式 | P1 |
| 24 | 平台佣金 | 0% | `绿标价 × commission_rate[类目]` | 一级类目 | P0 |
| 25 | 总成本 | 0% | 见 §二 | 采购价, 运费, 物流, 佣金, 广告 | P0 |
| 26 | 毛利率 | 0% (仅_est) | `(绿标价 - 总成本) / 绿标价` | 总成本 | P0 |
| 27 | 利润 | 0% | `绿标价 - 总成本` | 总成本 | P0 |
| 28 | 月销售额_est | 0% | `绿标价 × 月销量_est` | 月销量_est | P1 |
| 29 | 成本占比 | 0% | `总成本 / 绿标价` | 总成本 | P2 |

### 不可获取(放弃)

曝光次数, 详情页访问量, 真实月销量, 商品卡加购率, 搜索至购物车转化率, 推广费用占比 — **Ozon卖家后台私域数据**

---

## 二、数学建模公式

### 核心成本链(卢布核算)

```
计费重(kg) = max(实重_kg, 长_cm × 宽_cm × 高_cm / 5000)

1688采购成本 = 阶梯价(Q) × fx(CNY→RUB)

1688国内运费 = 运费模板值 × fx

Ozon物流费 = tariff_base[配送方式] + weight_rate × ceil(计费重, 0.5kg)
           + return_rate × return_fee_base

平台佣金 = 绿标价 × commission_rate[一级类目, 价格档]

总成本 = 1688采购成本 + 1688运费 + 头程清关 + Ozon物流费
       + 平台佣金 + 绿标价 × (广告率 + 损耗率 + 支付提现率)

利润 = 绿标价 - 总成本

毛利率 = 利润 / 绿标价 × 100%
```

### 月销量模型(不可爬取, 仅估算)

```
月销量_est = 0.6 × 评论_30d_delta / review_rate[类目]
           + 0.3 × (列表排名_position / 类目总商品数) × 类目月总销量_est
           + 0.1 × (last库存 - now库存)
```

评论购买比(行业经验值): 电子产品~1:20, 服装~1:5, 家居~1:10

### 转化率模型(estimator增强)

```
CVR_est = clip(
    base_cvr[类目] × exp(
        0.35 × (评分 - 4.5)
        + 0.08 × ln(1 + 评论数)
        - 0.70 × ln(绿标价 / 类目均价)
        - 0.25 × ln(1 + 跟卖数量)
        + 0.15 × 快速配送_布尔
    ),
    0, CVR_上限
)
```

当前 `estimator.py:26-65` 简化实现: `rating×0.5 + reviews×0.2 + price×0.2 + competition×0.1`

### 类目佣金费率表(需配置化)

| 一级类目 | 费率 | 备注 |
|---------|------|------|
| Электроника(电子产品) | 10% | 手机/电脑/耳机 |
| Бытовая техника(家用电器) | 12% | 吸尘器/微波炉 |
| Одежда(服装) | 15% | — |
| Красота(美容) | 13% | — |
| Товары для дома(家居) | 13% | — |
| default | 14% | config当前值 |

---

## 三、P0 立即修复(阻塞评分管道)

### P0-1: normalize_product 增加评论数字段
**文件**: `parser.py` → `normalize_product()` return dict
**现状**: parser提取了`reviews`但normalize丢弃, estimator读`评论数`读空气
**修复**: 增加 `"评论数": raw.get("reviews")` 到return dict
**预期**: 转换率估算质量翻倍, 为月销量模型提供输入

### P0-2: 提取 JSON-LD 类目
**文件**: `scraper.py` → `parse_search_page`或新函数
**现状**: JSON-LD `BreadcrumbList` 未被解析
**修复**: 
```python
import json, html as html_lib
ld_match = re.search(r'<script[^>]*type="application/ld\+json"[^>]*>(.*?)</script>', page_html, re.DOTALL)
if ld_match:
    data = json.loads(html_lib.unescape(ld_match.group(1)))
    if data.get('@type') == 'BreadcrumbList':
        items = data['itemListElement']
        level1 = items[0]['name'] if len(items) > 0 else None
        level3 = items[-1]['name'] if len(items) > 1 else None
```
**预期**: 一级/三级类目 0% → 80%+ (页面级数据)

### P0-3: merge_dataframes 增强字段回填
**文件**: `main.py:179-188`
**现状**: `existing_skus` 中的scraped行被丢弃, 增强数据丢失
**修复**: Excel行优先, 但将scraped行中非null字段回填到Excel行
**预期**: 同SKU的detail/1688数据不再丢失

### P0-4: 实现 1688 实时爬取
**文件**: `supplier_1688.py`
**现状**: `open_page_1688()`占位符, `fetch_supplier_fields()`仅读CSV
**修复**: 
1. `resolve_supplier_target()` 支持keyword搜索
2. 新增 `scrape_1688_product(keyword)` — Playwright搜索+详情
3. `enrich_products_with_supplier()` 调用实时爬取(非仅CSV)
**中文正则**: `[¥￥]\s*(\d+\.?\d*)`, `运费[:：]?\s*[¥￥]?\d+`, `起批`
**预期**: 采购价/运费 0% → 30-50% (依赖1688反爬情况)

### P0-5: estimator 增加评论数和类目费率
**文件**: `estimator.py:35,80-96`
**现状**: 读`评论数`但输入为None, `commission_rate_default`固定14%
**修复**: 增加`estimate_sales(row, cfg)` + 按类目查佣金表
**预期**: 转化率估算精度提升, 月销量_est可用

---

## 四、P1 次要修复

### P1-1: extract_detail_fields 重写
**文件**: `scraper.py:288-318`
**现状**: 面包屑/跟卖/销量正则全部失效
**修复**: 解析详情页JSON-LD + `data-state`属性, 搜索 `Артикул/Бренд/Вес/Продавец`

### P1-2: find_excel 优先 merged xlsx
**文件**: `main.py:41`
**现状**: `*.xls` 优先于 `*.xlsx`, 重启后加载原始数据
**修复**: `sorted(glob(...), key=lambda p: os.path.getmtime(p), reverse=True)`

### P1-3: 增加配送/重量/尺寸字段
**文件**: `parser.py` + `scraper.py`
**俄文**: `Доставка`, `Вес товара`, `Длина/Ширина/Высота`
**预期**: 物流费建模质量提升

---

## 五、依赖链

```
JSON-LD类目 ──→ 佣金费率查询 ──→ 总成本 ──→ 毛利率 ──→ 评分排序
                                        │
1688采购价 ──→ 总成本 ──────────────────┘
1688运费   ──→ 总成本 ──────────────────┘
Ozon重量   ──→ 物流费 ──→ 总成本 ────────┘
评论数     ──→ 月销量_est ──→ 评分排序
评论数     ──→ 转化率_est ──→ 评分排序
跟卖数量   ──→ 竞争度 ──→ 评分排序
```

**阻塞链**: 1688采购价(阻塞) → 总成本(阻塞) → 毛利率/利润(阻塞) → 评分排序失效
